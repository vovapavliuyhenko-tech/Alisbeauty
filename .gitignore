# OS / editor
.DS_Store
Thumbs.db
*.log

# Node (для локального serve / деплой-инструментов)
node_modules/
.vercel/

# Env (токены Telegram и пр. — только в окружении, не в репозитории)
.env
.env.local
.env*.local
